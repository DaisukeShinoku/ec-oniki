      <div class="sidebar_content">
        	<ul class="list-group">
            <li class="list-group-item list-group-item-primary text-center"><h5>エリア検索</h5></li>
            <% areas.each do |area| %>
              <%= link_to area.name, products_path(area_id: area.id), class: "list-group-item text-center" %>
            <% end %>
          </ul>
      </div>
      <div class="sidebar_content">
        	<ul class="list-group">
            <li class="list-group-item list-group-item-warning text-center"><h4>ジャンル検索</h4></li>
            <% categories.each do |category| %>
              <%= link_to category.name, products_path(category_id: category.id), class: "list-group-item text-center" %>
            <% end %>
          </ul>
      </div>